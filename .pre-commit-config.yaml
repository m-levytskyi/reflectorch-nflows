repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml

  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: ruff format
        language: system
        files: ^(reflectorch|tests)/.*\.py$
        types_or: [python, pyi]

      - id: ruff-check
        name: ruff check
        entry: ruff check --fix
        language: system
        files: ^(reflectorch|tests)/.*\.py$
        types_or: [python, pyi]

      - id: pytest-unit
        name: pytest unit
        entry: pytest -q tests/unit
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-push]

      - id: ty-check
        name: ty check
        entry: ty check reflectorch tests
        language: system
        always_run: true
        pass_filenames: false
        stages: [manual]
